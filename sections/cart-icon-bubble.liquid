{%- comment -%}
  Re-rendered into the header cart link (#cart-icon-bubble) on cart updates.
  Uses the shared cart-count snippet so the live count matches the header.
{%- endcomment -%}
{%- render 'cart-count' -%}
